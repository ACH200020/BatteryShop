@model CoreLayer.DTOs.Product.ProductImage.ImageFilterDto
@using CoreLayer.Utilities;
@{
    ViewData["title"] = "مدیریت عکس محصولات";
    ViewData["selected"] = "image";
    var counter = 1;
}
<section class="content-header">
    <h1>
        @ViewData["title"]
    </h1>
    <ol class="breadcrumb">
        <li><a asp-area="Admin" asp-controller="Category" asp-action="Index"><i class="fa fa-dashboard"></i> خانه</a></li>
        @*<li class="active">@ViewData["title"]</li>*@
    </ol>
</section>
<section class="content">
    @*<div class="box box-danger">
        <div class="box-body">
            <form>
                <div class="box-header">
                    <button type="submit" class="btn btn-info pull-left">فیلتر </button>
                </div>
                <div class="row">
                    <div class="col-md-6 form-group">
                        <input class="form-control" type="text" value="@Model.FilterParams.ProductTitle" placeholder="عنوان" name="producttitle" />
                    </div>
                    
                </div>
            </form>
        </div>
    </div>*@
    <a class="btn btn-success" style="margin-bottom: 5px" asp-area="Admin" asp-controller="ImageProduct" asp-action="Add">افزودن عکس</a>
    <div class="box box-primary ">
        <div class="box-body p-0">
            <table class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>عکس</th>
                        
                        <th>گروه</th>
                        
                        <th>عملیات</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.ImageDtos)
                    {
                        <tr>
                            <td>@counter</td>
                            <td>
                                <img style="max-width: 150px" src="@Directories.GetProductContentImage(item.ImageName)" />
                            </td>
                            <td>@item.Product.Title</td>
                            <td>
                                <a type="submit" asp-area="admin" asp-controller="ImageProduct" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm">حذف</a>
                            </td>
                        </tr>

                        counter += 1;
                    }
                </tbody>
            </table>
        </div>
        
    </div>
</section>